From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Vesper Materializer <vesper-materializer@users.noreply.github.com>
Date: Sat, 15 Aug 2026 08:32:16 +0000
Subject: [PATCH 07/21] fix: refine Vesper conversation appearance

Signed-off-by: Vesper Materializer <vesper-materializer@users.noreply.github.com>
---
 .../V2ConversationItemTextOnlyViewHolder.kt   |  5 +--
 .../securesms/keyvalue/SettingsValues.java    |  2 +-
 .../reactions/ReactionsConversationView.java  | 35 +++++++++++++++++--
 3 files changed, 37 insertions(+), 5 deletions(-)

diff --git a/app/src/main/java/org/thoughtcrime/securesms/conversation/v2/items/V2ConversationItemTextOnlyViewHolder.kt b/app/src/main/java/org/thoughtcrime/securesms/conversation/v2/items/V2ConversationItemTextOnlyViewHolder.kt
index 24cc490730..a21a43e16b 100644
--- a/app/src/main/java/org/thoughtcrime/securesms/conversation/v2/items/V2ConversationItemTextOnlyViewHolder.kt
+++ b/app/src/main/java/org/thoughtcrime/securesms/conversation/v2/items/V2ConversationItemTextOnlyViewHolder.kt
@@ -45,6 +45,7 @@ import org.thoughtcrime.securesms.database.model.MessageRecord
 import org.thoughtcrime.securesms.database.model.MmsMessageRecord
 import org.thoughtcrime.securesms.dependencies.AppDependencies
 import org.thoughtcrime.securesms.keyvalue.SignalStore
+import org.thoughtcrime.securesms.messages.PreservedRemoteDelete
 import org.thoughtcrime.securesms.recipients.Recipient
 import org.thoughtcrime.securesms.recipients.RecipientId
 import org.thoughtcrime.securesms.util.InterceptableLongClickCopyLinkSpan
@@ -693,14 +694,14 @@ open class V2ConversationItemTextOnlyViewHolder<Model : MappingModel<Model>>(
       binding.reactions.setReactions(conversationMessage.messageRecord.reactions)
       binding.root.addOnMeasureListener(reactionMeasureListener)
       binding.reactions.setOnClickListener {
-        if (conversationContext.selectedItems.isEmpty()) {
+        if (PreservedRemoteDelete.hasUserReactions(conversationMessage.messageRecord.reactions) && conversationContext.selectedItems.isEmpty()) {
           conversationContext.clickListener
             .onReactionClicked(
               Multiselect.getParts(conversationMessage).asSingle().singlePart,
               conversationMessage.messageRecord.id,
               conversationMessage.messageRecord.isMms
             )
-        } else {
+        } else if (conversationContext.selectedItems.isNotEmpty()) {
           conversationContext.clickListener.onItemClick(getMultiselectPartForLatestTouch())
         }
       }
diff --git a/app/src/main/java/org/thoughtcrime/securesms/keyvalue/SettingsValues.java b/app/src/main/java/org/thoughtcrime/securesms/keyvalue/SettingsValues.java
index 53537b8db2..520439795e 100644
--- a/app/src/main/java/org/thoughtcrime/securesms/keyvalue/SettingsValues.java
+++ b/app/src/main/java/org/thoughtcrime/securesms/keyvalue/SettingsValues.java
@@ -256,7 +256,7 @@ public final class SettingsValues extends SignalStoreValues {
   }
 
   public boolean isDynamicColorsEnabled() {
-    return getBoolean(DYNAMIC_COLORS_ENABLED, false);
+    return getBoolean(DYNAMIC_COLORS_ENABLED, true);
   }
 
   public int getMessageFontSize() {
diff --git a/app/src/main/java/org/thoughtcrime/securesms/reactions/ReactionsConversationView.java b/app/src/main/java/org/thoughtcrime/securesms/reactions/ReactionsConversationView.java
index 50570a6827..a7dcd2e132 100644
--- a/app/src/main/java/org/thoughtcrime/securesms/reactions/ReactionsConversationView.java
+++ b/app/src/main/java/org/thoughtcrime/securesms/reactions/ReactionsConversationView.java
@@ -1,6 +1,7 @@
 package org.thoughtcrime.securesms.reactions;
 
 import android.content.Context;
+import android.content.res.ColorStateList;
 import android.content.res.TypedArray;
 import android.util.AttributeSet;
 import android.view.LayoutInflater;
@@ -12,6 +13,7 @@ import android.widget.TextView;
 import androidx.annotation.NonNull;
 import androidx.annotation.Nullable;
 import androidx.core.content.ContextCompat;
+import androidx.core.widget.TextViewCompat;
 
 import org.signal.core.ui.util.ThemeUtil;
 import org.thoughtcrime.securesms.R;
@@ -37,6 +39,8 @@ public class ReactionsConversationView extends LinearLayout {
   private List<ReactionRecord> records;
   private int                  bubbleWidth;
   private boolean              isSettingBubbleWidth;
+  private @Nullable View       preservedDeletePill;
+  private @Nullable TextView   preservedDeleteLabel;
 
   public ReactionsConversationView(Context context) {
     super(context);
@@ -60,6 +64,8 @@ public class ReactionsConversationView extends LinearLayout {
   public void clear() {
     this.records.clear();
     this.bubbleWidth = 0;
+    this.preservedDeletePill = null;
+    this.preservedDeleteLabel = null;
     removeAllViews();
   }
 
@@ -87,6 +93,13 @@ public class ReactionsConversationView extends LinearLayout {
         pill.setVisibility(bubbleWidth == 0 ? INVISIBLE : VISIBLE);
       }
 
+      setPreservedDeleteCompact(false);
+
+      if (preservedDeletePill != null) {
+        preservedDeletePill.measure(MeasureSpec.makeMeasureSpec(0, MeasureSpec.UNSPECIFIED), MeasureSpec.makeMeasureSpec(0, MeasureSpec.UNSPECIFIED));
+        setPreservedDeleteCompact(preservedDeletePill.getMeasuredWidth() >= bubbleWidth - OUTER_MARGIN);
+      }
+
       measure(MeasureSpec.makeMeasureSpec(0, MeasureSpec.UNSPECIFIED), MeasureSpec.makeMeasureSpec(0, MeasureSpec.UNSPECIFIED));
 
       int railWidth = getMeasuredWidth();
@@ -129,9 +142,13 @@ public class ReactionsConversationView extends LinearLayout {
     List<Reaction> reactions = buildSortedReactionsList(records);
 
     removeAllViews();
+    preservedDeletePill = null;
+    preservedDeleteLabel = null;
 
     if (PreservedRemoteDelete.hasMarker(records)) {
-      addView(buildPreservedDeletePill(getContext(), this));
+      preservedDeletePill = buildPreservedDeletePill(getContext(), this);
+      preservedDeleteLabel = preservedDeletePill.findViewById(R.id.reactions_pill_count);
+      addView(preservedDeletePill);
     }
 
     for (Reaction reaction : reactions) {
@@ -238,14 +255,28 @@ public class ReactionsConversationView extends LinearLayout {
 
     emojiView.setVisibility(GONE);
     spacer.setVisibility(GONE);
+    int foregroundColor = ThemeUtil.getThemedColor(context, com.google.android.material.R.attr.colorOnErrorContainer);
+
     countView.setText(R.string.preferences__preserved_remote_delete_badge);
-    countView.setTextColor(ThemeUtil.getThemedColor(context, com.google.android.material.R.attr.colorOnErrorContainer));
+    countView.setTextColor(foregroundColor);
+    countView.setCompoundDrawablesRelativeWithIntrinsicBounds(R.drawable.symbol_trash_compact_20, 0, 0, 0);
+    countView.setCompoundDrawablePadding(ViewUtil.dpToPx(4));
+    TextViewCompat.setCompoundDrawableTintList(countView, ColorStateList.valueOf(foregroundColor));
     root.setBackground(ContextCompat.getDrawable(context, R.drawable.vesper_preserved_delete_pill_background));
     root.setContentDescription(context.getString(R.string.preferences__preserved_remote_delete_badge));
 
     return root;
   }
 
+  private void setPreservedDeleteCompact(boolean compact) {
+    if (preservedDeleteLabel == null) {
+      return;
+    }
+
+    preservedDeleteLabel.setText(compact ? null : getContext().getString(R.string.preferences__preserved_remote_delete_badge));
+    preservedDeleteLabel.setCompoundDrawablePadding(compact ? 0 : ViewUtil.dpToPx(4));
+  }
+
   private static class Reaction implements Comparable<Reaction> {
     private String  baseEmoji;
     private String  displayEmoji;
